import { AppLayout } from "@/components/layout/AppLayout";

export default function PortfolioBreakdown() {
  return (
    <AppLayout title="Portfolio Breakdown" subtitle="Gedetailleerde analyse van je portefeuille">
      <div className="text-muted-foreground">Portfolio Breakdown — binnenkort beschikbaar.</div>
    </AppLayout>
  );
}
