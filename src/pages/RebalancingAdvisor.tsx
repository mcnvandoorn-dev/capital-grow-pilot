import { AppLayout } from "@/components/layout/AppLayout";

export default function RebalancingAdvisor() {
  return (
    <AppLayout title="Rebalancing Advisor" subtitle="Herbalanceer-advies op basis van je strategie">
      <div className="text-muted-foreground">Rebalancing Advisor — binnenkort beschikbaar.</div>
    </AppLayout>
  );
}
